#include "binary_trees.h"
/**
 * binary_trr_depth - measures the depth of a node in a binary tree
 * @tree: a pointer to the root node 
 * Return: 0 if the tree is NULL
*/
size_t binary_tree_depth(const binary_tree_t *tree)
{
    
}